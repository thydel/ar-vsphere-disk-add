---

factsd: /etc/ansible/facts.d
udevrule: /etc/udev/rules.d/65-my-persistent-scsi.rules

vcenter:  "{{ vcenter_hostname }}"
username: "{{ vcenter_username }}"

vmname:   "{{ inventory_hostname }}.{{ vcenter_fqdn }}"

etc:
  crypttab: /etc/crypttab
